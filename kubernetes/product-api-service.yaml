apiVersion: v1
kind: Service
metadata:
  name: product-api
  labels:
    app: product-api
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: product-api